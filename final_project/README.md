# GigaVibeMiptCode

Решение лежит в папке `project`.

```bash
cd project
python -m pip install -r requirements.txt
python src/main.py
```

Основное описание, примеры конфигурации и команды проверки есть в `project/README.md`.
